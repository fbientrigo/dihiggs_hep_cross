# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local outputs / generated artifacts
outputs/
*.log

# Local, user-supplied / generated sigma-input data (see data/manual/README.md).
# These are local inputs or generator outputs, not repo content; keep the README.
data/manual/*.csv

# Drop-in directory for real 2HDMC scan files (see data/2hdmc_scans/README.md).
# Keep the README; ignore any real/large scan data placed here.
data/2hdmc_scans/*
!data/2hdmc_scans/README.md

# Large/bootstrap archives
*.tar.gz
*.zip

# OS/editor
.DS_Store
.vscode/
.idea/
